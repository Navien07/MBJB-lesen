# Continue here — session handoff (2026-07-29)

Canonical project state lives in `docs/STATE.md` (gates M0–M9 + verify.sh green,
production live, M10 smoke blocked on Anthropic credits). This file captures the
IN-FLIGHT task only.

## In-flight task: make production a working real-time demo

User request: real sample files to drag-and-drop, remove the fake seeded queue,
keep only a working end-to-end demo. Production pipeline currently parks because
the Anthropic key has no credits, so everything sits at "Intake check".

### Done
- `scripts/generate-demo-documents.ts` → `public/demo-docs/` (8 SPECIMEN files:
  ssm-cert, cukai-harta-2026, dbp-approval, tenancy, mykad, floorplan PNGs +
  `demo-board-086.png` and `signboard-lowres.png` copied from M6 fixtures).
  IMPORTANT: signboard filenames drive the replay simulator — `demo-board-086.png`
  → 0.86 ratio failure; anything matching /lowres/ → escalation.

### Remaining (in order)
1. Add a "Demo documents" download card to `app/applications/[id]/page.tsx`
   (visible when editable) linking the 8 files in `/demo-docs/`.
2. Fix officer dashboard deficiency-reasons query to ignore audit rows whose
   application no longer exists (cleanup orphans them; audit is append-only by
   design and cannot be deleted).
3. Write `scripts/reset-demo.ts`: delete ALL remote applications (cascade cleans
   children; keep auth users officer.demo + applicant-demo-*), then reseed ONE
   demo case: Kedai Runcit Aman Jaya, applicant-demo-99, SUBMITTED, docs minus
   DOC-DBP, artwork `demo-board-086.png` uploaded to storage, intake job queued.
4. Flip production env `AI_GATEWAY_MODE` to `replay` (vercel env rm/add, then
   redeploy — env changes need a new deployment). Note in STATE.md: flip back to
   `live` once credits are added.
5. Run reset-demo against remote (SUPABASE_URL + SERVICE key via
   `supabase projects api-keys --project-ref ugbfvxjmdgznriuuedvn`).
6. Verify in prod (Playwright, PLAYWRIGHT_BASE_URL): Pemohon demo button → open
   case → deficiency notice names DBP → upload dbp-approval.png → resubmit →
   ASSESSED; officer side shows findings. Then typecheck/lint/test:e2e, commit.

### Demo flow that must work at the end
Pemohon button → case DEFICIENT with DBP named → download dbp-approval.png from
the new card → upload → resubmit → watch 4 stages → ASSESSED. Pegawai button →
queue has the one case → findings with 0.86 evidence → override needs reason →
decide → dashboard + replay populate.
